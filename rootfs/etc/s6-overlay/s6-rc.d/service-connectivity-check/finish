#!/command/with-contenv bash
# shellcheck shell=bash

# Tunnel down: halt so Docker restarts and re-establishes it. Safe because the
# kill switch blocks all traffic while the tunnel is down.
/run/s6/basedir/bin/halt
