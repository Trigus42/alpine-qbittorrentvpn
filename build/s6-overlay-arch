#!/bin/bash

TARGET_ARCH="$(uname -m)"

if [ "$TARGET_ARCH" = "x86_64" ]; then
    echo "amd64"
elif [ "$TARGET_ARCH" = "armv7l" ]; then
    echo "armhf"
elif [ "$TARGET_ARCH" = "aarch64" ]; then
    echo "aarch64"
fi